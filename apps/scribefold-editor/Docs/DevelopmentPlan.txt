my fitness pal 
calories tracker and bmi calculator

Overal plan and timeline for the development of the application.

Then and Now

    Previously
        did not create a plan
        did not lay out file structure, data flows, user stories etc before
        did not set timelines

        so always pushing as fast as I can
        cutting cornders and moving on to next thing as soon as current on at minimum viable 
        unsustainably spending time on it until burning out

        put everything else on pause to fosuc on it entirely 
        and it was so long all at once it could not be completed before other thigns piled up and needed to stop
        if didn't complete the entire thing it was a failure

        trying to do the whole thing at once in one big bite
        can't eat an entire steak in one bite
        but you can break it into many piecs and finish it fine

    Better:
        create a plan for phases
        create a timeline for each phase
        do not progress past that so it stays sustainable
            when you are done with the weeks work you do other things that need to be done and to balance life so you can carry on
        if you spend the number of weeks on it you predict you will have the finished product at the time you expect



        Each phase has a part that is complete
        typing out the user interactions stream of consciousness
        while doing that typing out the components with their displays and state interactions
        and in the 3rd window have the file structure 
        create files based on files strucutre
        put psudocode notes at the top of each
        fill them in (ai can do this becaues its by your plan and you can do in small increments looking it over for spaghetti)

Phases: 

    ScribeFold AI Development Plan
    Starting 2026-1-1
    10 weeks of work
    2 weeks of vacation time used as needed
    12 weeks total (3 months)
    +1 month of prototype time (already done)
    so 4 months total on the development 
    then 4 months post development to make use of it  
    So 8 months total for the end result

    1 day
    UI (done) 
        main area ui        (done)
            background      (done)
            page            (done)
            ai side bar     (done)
        topbar              (done)
        ai chat area layout (done)

    1 day 
    settings and windows (done)
        f11 mode                    (done)
        ai mode on / off            (done)
        windows slice               (done)
            just showing windows    (done)
            settings window opens   (done)
            help button shows help window (done)
        settings ui
            tabs
                tabs UI                     (done) 
                tabs change what shows      (done)
                components for each         (done)
                UI for each component       (done)
                components change settingsObject (done) 
                they display based on the selected tab (done)
                tab component UIs           (done)

    2 days
    file IO  (done)
        load file (done)
        save file (done)
        save as (done)
        reopen file on close/reopen (done)
        works in desktop and browser  (done)
            may be different top menu options in browser
            may need the explanation window to download file instead of save on browser
        
    2 days 
    ai chat area  (done)
        sends to api (done)
            after gathering from the editor ref (done)
            just getting a response, no user token updates now (done)
        gets response, prases it, puts it into redux (done)
            just logs it for now (done)

    code mirror (mostly done)
        ctrl f
        spell check

    connecting code mirror and ai chat responses (done)
        logging just shown parts with ids
        getting response based on that
        logging it
        showing it in the diff view
        returning to non diff view when all are accepted/rejected
        diff navigator
            searches for all elements that have the css accept/reject button area class 
                and loops thorugh them
                changing css and scrolling them into view
                mabye call same function when accept/reject is clicked   

    1 week
    general use, bug resolution, tweaks
        settings categories
        help text
        little bugs like next/prev wrapping etc

    2 days
    dev ops (done)
        render.com 
            api (done)
                create web service from apps/scribefold-api with same settins as other api (done)
                test in url (running)
            web build
                create static site with apps/scribefold-editor 
            web portal
                ensure it points to the correct web build 
                    test
                        api (running)
                        web build 
                downloads 
                    from yaml and script 
                    windows, linux, mac  
            web build 
                working in web, linked from the web portal 
        testing (todo)
            test installs:
                windows 
                linux
                mac
        github build pipeline that places the downloadables onto the web portal (done)
            todo: test download installs on each os 
        maybe move away from monorepo
        
    1 week
    server basic functionality 
        chat api (done)
            sends proper response 
            tokens 
                loads tokens 
                does request based on tokens 
                adjusts tokens
                responds so front end can update its tokens (maybe just send new available tokens)
        new acount token claiming flow (done)
            create account 
                creates account 
                creates db user
                sends email
                    with tokens 
            token confirmation
                adds tokens 
                loggs in user 
        stripe  (todo)
            montly  
            one time 
        
    stripe subscriptions (done)
        working on 
            get stripe set up 
            get env variables 
              check the env examples, 
              make sure all there, 
              organize and comment, 
              keep only necessary ones
            checkout flow (working)
            push and test webhook calling 
                push 
                go to hosted web portal and do a checkout 
                check hosted server logs to see if the correct loggs show 
                if correct for each event can move on to adding correct code for each event  
        checkout
            actions:
                add a subscription
                canel a subscription
                downgrade 
                upgrade
            flows:
                user goes to the portal, loggs in, adds a subscription for the first time
                the payment is successful and then the webhook triggers the points being added
                a month later the payment is auto charged and that webhook is triggered to add the tokens again 
                the user decides to upgrade 
                they can upgrade, pay only the difference, and only the difference in tokens is added 
                a month later it auto charges and adds the proper tokesn for that 
                later they downgrade again 
                it does not charge or takes tokens but next billing cycle it will charge the lower amount and add the proper tokens amount 
                they can cancel this 
            adds the proper number of tokens in each case 
            shows their current subscription status right away in web portal 
                the api will respond with information to update it
            shows in the electron app (after refresh is ok)
            steps:
                create and setup stripe
                    create account (done)
                    go into test mode (done)
                    add webhook endpoint (done)
                    add products (done)
                    other things?
                gather env variables and urls 
                    url, public and private keys, product ids, ?
                link web portal to work with stripe
                    maybe create an endpoint and use that  
                create code to update supabase properly in each situation 
 
    1 week
    referrals feature
        First Promoter (saas)
            Create account 
                get api keys 
            user subscription server code creates a user for it and stores that ref id in their user data
                so call the fp api sending it the relivant data 
                this then shows on the web portal 
                a token to log into their fp web portal is generated and put in user data 
                    only accessed thorugh server after verifying sent jwt so same security as being logged into supabase   
                they send the link to someone 
            the web portal: 
                extracts the ref id
                puts it in local storage
                checks in local storage to see if it registered the click with fp and if not calls an api endpoint to do so 
                cslling fp /track/signup '
                    {
                        "ref_id": "test37we1lts",
                        "email": "user@example.com",
                        "uid": "your-user-uuid-12345"  // optional but recommended
                    }
                    If emails differ (e.g., user signs up with one email, pays with another), attribution fails unless you use uid matching.
                        so maybe use uid as well 
                        maybe send thigns like ip address etc to help with detection                         
            if user downloads 
                stores the ref id in a text file in the zip 
                so need download to have a button that requests and sends the ref id in localstorage 
                then if user creates an account in the desktop app the referral id is added to their account 
            so at this point we have regeistered a click and if they create an account in web or desktop the ref id is stored in their user data to show who referred them 
            registering a payment 
                could do it with manual code in the webhook (not reccomended becaus other stuff might not work like refunds etc)
                when there is a person referred with an email associated then someone wiht that email subscribes it knows?    
                    FP matches these to referrals using:
                        Email (from Stripe customer → must match the email you sent in prior /track/signup or lead call).
                        UID (your internal user ID, if you send it via tracking script/API).
                        Coupon codes (if using Stripe coupons tied to promoters — FP supports native coupon tracking).
            dashboard 
                the referral link will be in user data so can be displayed already 
                maybe the other stuff like 
                    number of clicks 
                    number of downloads 
                    number of subscribers 
                    cancellations 
                    amount owed 
                    when they can collect it 
                    request payment button 
                    where to pay it to 

        Tracking referred by 
            when user downloads application referral id is tracked somehow
            maybe from their ip on download, and then if they use from that ip for first time around the same time with granted credits we can link that account to that ref id
            and then if they subscribe we will know where the referral came from
            maybe save in the storage of the device then the first user to log in there is associated with that referrer by adding the ref id in the users data 
        Dashboard
            have referral accounts
            show 
                number of downloads
                number and percent that then subscribed
                percentage they get
                    and terms etc in an info window
                calculation of what the percentage times subscriptions amounts are
                graph of how many by date for each thing with recharts (downloads, subscriptions, etc)
            reminder they can do this in a few places
            auto adding to accounts what they got in referrals
            get the percentages
                need a sub for that
                it adds up to being worth it to have it
            Design 
                referrals table 
                    on account creation user gets a row in the Referrals table 
                        they are given a unique referral id in this function which is added to that table row when its created 
                        rows have arrays of user ids for each category 
                            clicks, downloads, subscribed (maybe row for each subscription tier), etc 
                add to array rpc function 
                    when user clicks link and there is a referral id it will try to update the referrals.ref_id.user_clicks
                        this will only add their id if its not already in there 
                        this might get computationally expensive though to do this every time they go to the website 
                            maybe also have a localstorage value to say tallied_click_for_referrall=ref_id that is checked before doing that
                                if the current referral id is already in that value then the tally api call is not made 
                                can do same for download, etc 
                keeping track of payments is key though 
                    this is more complicated because the user could subscribe and then unsubscribe, 
                    could use different referral ids at different times, 
                    needs to be on paid and save the payment date because funds are only available 30 days later becasue of possible cancelations and prorating that coudl be used to abuse the referal payments system 
                        sould we keep track of this with another table? 
                            payments and the referral id would be one of them 
                            and this needs to reflect the id arrays in the referrals table, 
                                if a referrer has a user in their array then another referrer does the same and the referral id in the subscriber is only the newer one then the first referrer will see that user as someone they referred and who is subscribed but they wont see any payments 
                    when a user has a payment successful call with the customer id, which is then linked to the auth id 
                        we need a way to use that auth id or user id to add to the balance of the referrer 
                    maybe we have a referrals dashboard tab in the web portal 
                        has detailed info like counts for each stat  
                        list of users they referred 
                            they can have usernames if they want, users and referrers 
                            can look up any user id or name and see who referred them and what their subscription status is 

            a unique id is assigned to them and put in their referral row along with their id 
                custom referral link, maybe for different tier, maybe add later 
                the custom one can be a secondary value that is also used to look up the row 
                    query: if x == referral_id or x == custom_referral_id
            user that uses to referral link
                ref id is saved in localstorage 
                if they create an account localstorage is checked and refid is sent if exists 
                if a download happens localstorage is checked and refid send if exists  
            incrementing values 
                using rpc server functions to prevent race conditions 
                clicks, downloads, payments, referred clicks user ids array (ids)
                    maybe they can all be id arrays. 
                        count of array would be the number 
                        then we will have a record of who referred for what too 

            
    
    1 week
    general use and bug resolution
    
    cleanup 
        settings tabs orgaization
        remoce logs 
        unused files
        clean up codebase  
   
    security
        supabase table rls (done)
        scan through for vulnerabilities (do more of this)
            maybe add rate limiting for the account creation or magic links from same ip
            also shold make it so go to web portal is not clickable while processing so multiples are not created 
                (and maybe a processing message for that) 


    2 days
    the desktop on start reminder      

    2 weeks (maybe longer)
    prompt preface and story type sliders
        so figuring out what makes a story interesting and emotinally engaging
        and having sliders for differnet types
        "training the ai" with propmpt prefaces 



    phase 1:
        Prototype
        (1 month) 2025-11-14 to 2025-12-14 (done)
        Tasks:
            creata a basic prototype 
            vibe coding, manualy, expreiments, docs etc
            this is meant to be a general layout and fact finding expreiments
            so we know what features we want and what tools are best
            it gives the understanding necessary to create the plan

    phase 2:
        Planning 
        (2 weeks) 2025-12-4 to 2025-12-31
        Tasks: 
            Define UI 
                Basic visual structure of the app 
                maybe make the files and have a comment at the top describing each one
                then can add the desription of the state and flows in the file 
            Usage stories
                What the user will do and what will happen
            Folder and file structure
            Data structures / database / api endpoints
            This is hard to do on its own without building anything 

    phase 3: 
        UI (2 weeks) 2026-1-1 to 2026-1-14
        Tasks:
            Create basic UI and file structure
                Can tell ai to create folders and files as per instruction 
                    in phases
                    making it look the same as the last version but in new files
                    keep very simple and watch carefully
            Wire up basic menu ui redux state
                Ex: Settigns slice then settings menu showing when it should, showing correct tabs and tab UI etc

    phase 3:
        implement data flows (2 weeks) 2026-1-14 to 2026-1-30
        create, load, display data as per the plan 
        creating or updating the api endpoints to recieve and give correct data

    Phase 4:
        testing, bug resolutions, updating plan (2 weeks) 2026-2-1 to 2026-2-30
        test the applicaiton, resolve bugs

Notes:
    As we go thorugh creating the plan 
    update the plan, file structure, database, data flows etc







Monaco prompt:
please tell me how to install monaco into a react electron web app. 
I also want to modify it in these ways:
sticky top bar on/off based on settings
same with the preview scrollbar
and line numbers in the gutters
custom language for folding on #section and #chapter tags with the section being inside of a chapter so if the parent chapter is folded the sections in it are hidden

I want to add ids to the lines
so I can pull the data from the editor
send it to an endpoint
get a response and then use the ids to add the content to the correct lines even if their number or order changes
and then have those two versions show as a dif in the monaco view
and of course adding new ids as new lines are added
and to stay very performant doing O(1) or low complexity operations

I want to have a custom glyph in the gutter that when clicked changes something correlated with that line so the glyph image changes (like a toggle)
and I want that change to be visible in the function that pulls the data and sends it to the api described previously

so it will be monaco
1: with custom css styles (transparent background etc)
2: conditional sticky top, gutter index values, and preview slider on the left (all based on a settingsObject json)
3: ability to have unique ids on each line that persist as the same for at least the entire session (does't have to persist on refresh or program load unless its easy to do so)
4: ability to have the data pulled from it for processing by an api with line numbers and a custom show or hide attribute that tells the function which lines to send to the api
5: ability to show diff of the original content and the content created when the api responds