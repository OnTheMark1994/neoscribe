/*
  Shows the message content

  When settingsObject.developerMode is true it has a little + square button at the top right 
    that dispatches an action that causes a MessageDetail window to show by setting menuSlice.messageDetailData 
 
*/
export default function Message() {
  return (
    <div>
      Message
    </div>
  );
}
