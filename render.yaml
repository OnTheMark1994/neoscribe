services:
  # API Server – Dynamic Node.js
  - type: web
    name: scribefold-ai-api
    runtime: node
    region: oregon
    plan: starter
    rootDir: apps/api-server
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /api/health
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: "3000"

  # Web Portal – Static site (FIXED: No region for static)
  - type: web
    name: scribefold-ai-web-portal
    runtime: static
    plan: starter
    rootDir: apps/web-portal
    buildCommand: npm install && npm run build
    staticPublishPath: ./build
    envVars:
      - key: NODE_ENV
        value: production

# Add your secrets only in the Render dashboard, never here